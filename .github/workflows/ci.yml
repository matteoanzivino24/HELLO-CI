name: CI Pipleine
on: 
  push:
    branches: ["main"]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4

      - name: Print Message
        run: echo "Benvenuti nella CI!"