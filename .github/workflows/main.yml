name: BelloIlBardo
on: 
  push:
    branches: ["release"]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4

      - name: Print Message
        run: echo "io sono Bello il Bardo"